#include <stdio.h>
/**
 * main - A c progam that prints the same value with put function
 * Return: 0 (success)
 */
int main(void)
{
puts("\"Programming is like building a multilingual puzzle");
return (0);
}
