#include <stdio.h>

/**
 * main - printing all numbers of base 16.
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int i;

	for (i = 0; i < 16; i++)
	{
		putchar(i);
	}
	putchar('\n');
	return (0);
}
