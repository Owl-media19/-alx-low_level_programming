#include <stdio.h>

/**
*abs - Computes the absolute value of an int
*
* @i: accepts number as  int
*
* Return: absolute value
*/
int main(void)
{
	int i;

	if (i >= 0)
	{
		return (i);
	}
	else
	{
		return (i * -1);
	}
}
