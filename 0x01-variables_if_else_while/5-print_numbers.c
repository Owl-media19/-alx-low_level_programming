#include <stdio.h>
#include <stdlib.h>
#include <time.h>
/**
 * main - to print numbers
 *
 * Return: Always 0 (success)
 */
int main(void)
{
	int i;

	for (i = 48; i < 52; i++)

		putchar(i);
		i++;
		putchar('\n');
	return (0);
}
